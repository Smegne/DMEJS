<%- include('../partials/header') %>

<div class="container mt-5">
    <h1 class="text-center mb-4">📊 Admin Dashboard</h1>

    <div class="row g-4">
        <!-- Total Users -->
        <div class="col-md-4">
            <div class="card shadow border-0 rounded-4 text-center bg-light h-100">
                <div class="card-body">
                    <div class="mb-3">
                        <i class="bi bi-people-fill fs-1 text-primary"></i>
                    </div>
                    <h5 class="card-title text-secondary">Total Users</h5>
                    <p class="card-text fs-3 fw-bold text-dark"><%= total_users %></p>
                </div>
            </div>
        </div>

        <!-- Total Students -->
        <div class="col-md-4">
            <div class="card shadow border-0 rounded-4 text-center bg-light h-100">
                <div class="card-body">
                    <div class="mb-3">
                        <i class="bi bi-mortarboard-fill fs-1 text-success"></i>
                    </div>
                    <h5 class="card-title text-secondary">Total Students</h5>
                    <p class="card-text fs-3 fw-bold text-dark"><%= total_students %></p>
                </div>
            </div>
        </div>

        <!-- Total Courses -->
        <div class="col-md-4">
            <div class="card shadow border-0 rounded-4 text-center bg-light h-100">
                <div class="card-body">
                    <div class="mb-3">
                        <i class="bi bi-journal-bookmark-fill fs-1 text-warning"></i>
                    </div>
                    <h5 class="card-title text-secondary">Total Courses</h5>
                    <p class="card-text fs-3 fw-bold text-dark"><%= total_courses %></p>
                </div>
            </div>
        </div>
    </div>
</div>

<%- include('../partials/footer') %>

<!-- Bootstrap Icons CDN -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
